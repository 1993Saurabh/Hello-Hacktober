#include <stdio.h>

void main() {printf("Hello, World!");}


// c program for various arithematic operations

#include <stdio.h>
#include <math.h>

int main() {
  
  int a, b; printf(" %d %d ", &a, &b);
  
  int c = a + b;
  int d = a - b;
  int e = a * b;
  int f = a / b;
  int g = a % b;
  int h = a ^ b;
  int i = Math.pow(a, b);
  int j = Math.sqrt(a);
  
  printf(" %d %d %d %d %d %d %d %d ", c, d, e, f, g, h, i, j);
  
 return 0; 
}

